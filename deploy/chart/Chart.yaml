apiVersion: v2
name: anvilkit-agent-workflow
description: The AnvilKit Agent Workflow worker (anvilkit-agent-workflow) - the service's own chart with the launcher's least-privilege identity; environment values and the pinned deployment combination belong to the deploying repository.
type: application
version: 0.1.0
appVersion: "0.1.0"
